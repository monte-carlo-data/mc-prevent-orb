version: 2.1
description: |
    Monte Carlo CI Gate — assess data pipeline risk before merge. Calls the Monte Carlo CI Gate API to evaluate pull request changes against your data observability signals (alerts, lineage, monitor coverage) and returns a pass/warn/fail verdict.
display:
    home_url: https://www.montecarlodata.com
    source_url: https://github.com/monte-carlo-data/mc-prevent-orb
commands:
    assess:
        description: |
            Run MC Prevent to assess data pipeline risk for a pull request. Polls for the PR agent assessment, applies the gate policy, and reports a verdict. Caches the verdict so subsequent commits reuse it without polling.
        parameters:
            api-url:
                default: https://api.getmontecarlo.com/ci/assess
                description: |
                    Monte Carlo MC Prevent API URL.
                type: string
            block-on:
                default: ""
                description: |
                    Which risk tiers cause the job to exit non-zero: low+ (block on low, medium, and high), medium+ (block on medium and high), high+ (block on high only), or none (never block). When left as the empty string this parameter is not sent to the backend, and the Monte Carlo UI setting applies.
                type: string
            fail-on:
                default: ""
                description: |
                    Deprecated — use block-on instead. Accepted values: warn_and_fail, fail_only, none. block-on takes precedence when both are set.
                type: string
            fail-on-error:
                default: true
                description: |
                    Deprecated — use block-on instead. false = block-on: none. true is a no-op (defers to block-on).
                type: boolean
            max-wait:
                default: 300
                description: |
                    Maximum seconds to wait for PR agent assessment before giving up.
                type: integer
            poll-interval:
                default: 30
                description: |
                    Seconds between poll attempts when waiting for PR agent assessment.
                type: integer
        steps:
            - restore_cache:
                keys:
                    - mc-prevent-{{ .Branch }}-
            - run:
                command: |
                    if [ -z "$CIRCLE_PULL_REQUEST" ]; then
                      echo "Not a PR build. Skipping."
                      exit 0
                    fi

                    MCD_ID="${MCD_DEFAULT_API_ID}"
                    MCD_TK="${MCD_DEFAULT_API_TOKEN}"
                    if [ -z "$MCD_ID" ] || [ -z "$MCD_TK" ]; then
                      echo "Credentials not configured."
                      exit 0
                    fi

                    PR=$(echo "$CIRCLE_PULL_REQUEST" \
                      | grep -oE '[0-9]+$')
                    OWN="${CIRCLE_PROJECT_USERNAME}"
                    RNM="${CIRCLE_PROJECT_REPONAME}"
                    REPO="${OWN}/${RNM}"
                    CF="/tmp/mc-prevent-verdict.json"

                    # Resolve effective block-on. Precedence:
                    # 1. block-on (new parameter)
                    # 2. fail-on (deprecated — backward compat)
                    # 3. fail-on-error (deprecated shim: false → none)
                    # When empty, the backend's UI setting applies.
                    EFF="$BLOCK_ON"
                    if [ -z "$EFF" ]; then
                      EFF="$FAIL_ON"
                    fi
                    if [ -z "$EFF" ]; then
                      if [ "$FAIL_ON_ERROR" = "false" ] \
                        || [ "$FAIL_ON_ERROR" = "0" ]; then
                        EFF="none"
                      fi
                    fi

                    echo "PR #${PR} on ${REPO}"
                    echo "Retrieving assessment..."
                    sleep "$POLL_INTERVAL"
                    WAITED="$POLL_INTERVAL"

                    while true; do
                      RESP=$(curl -s -w "\n%{http_code}" \
                        --max-time 45 \
                        -X POST "${MC_API_URL}" \
                        -H "x-mcd-id: ${MCD_ID}" \
                        -H "x-mcd-token: ${MCD_TK}" \
                        -H "Content-Type: application/json" \
                        -d "$(jq -n \
                          --arg r "$REPO" \
                          --argjson p "$PR" \
                          --arg s "$CIRCLE_SHA1" \
                          --arg block_on "$EFF" \
                          '{
                            repository: $r,
                            pr_number: $p,
                            commit_sha: $s
                          }
                          | if $block_on != "" then
                              . + {block_on: $block_on}
                            else . end')" 2>/dev/null) || true

                      HC=$(echo "$RESP" | tail -1)
                      BD=$(echo "$RESP" | sed '$d')

                      if ! echo "$BD" \
                        | jq . >/dev/null 2>&1; then
                        BD=""
                        SRC="error"
                      else
                        SRC=$(echo "$BD" \
                          | jq -r \
                            '.assessment_source // "error"')
                      fi

                      if [ "$SRC" != "none" ] \
                        && [ "$SRC" != "error" ]; then
                        echo "$BD" > "$CF"
                        break
                      fi

                      if [ -f "$CF" ]; then
                        echo "Using cached assessment."
                        BD=$(cat "$CF")
                        HC=200
                        SRC=$(echo "$BD" \
                          | jq -r \
                            '.assessment_source // "cached"')
                        break
                      fi

                      WAITED=$((WAITED + POLL_INTERVAL))
                      if [ "$WAITED" -ge "$MAX_WAIT" ]; then
                        echo "No assessment available."
                        break
                      fi

                      sleep "$POLL_INTERVAL"
                    done

                    echo "$BD" \
                      > /tmp/mc-prevent-raw-response.json

                    CL=$(echo "$BD" | jq -r \
                      '.conclusion // "error"' \
                      2>/dev/null || echo "error")
                    RT=$(echo "$BD" | jq -r \
                      '.risk_tier // "none"' \
                      2>/dev/null || echo "none")
                    HS=$(echo "$BD" | jq -r \
                      '.human_summary // ""' \
                      2>/dev/null || echo "")
                    SM=$(echo "$BD" | jq -r \
                      '.summary // ""' \
                      2>/dev/null || echo "")
                    DS="${HS:-$SM}"
                    PS=$(echo "$DS" \
                      | sed 's/\*\*//g; s/\*//g; s/`//g')

                    echo ""
                    echo "==========================="
                    echo " MC Prevent CI Check Result"
                    echo "==========================="
                    echo ""
                    echo "Conclusion: ${CL}"
                    echo "Risk tier:  ${RT}"
                    echo ""
                    echo "${PS}"
                    echo ""
                    echo "==========================="

                    # The backend has already merged CI and UI block_on and applied
                    # it to the verdict, so we just exit based on the final conclusion.
                    if [ "$CL" = "fail" ]; then
                      echo ""
                      echo "FAIL. Add mc-override."
                      exit 1
                    elif [ "$CL" = "warn" ]; then
                      echo ""
                      echo "WARNING. Review first."
                      exit 1
                    fi
                environment:
                    BLOCK_ON: << parameters.block-on >>
                    FAIL_ON: << parameters.fail-on >>
                    FAIL_ON_ERROR: << parameters.fail-on-error >>
                    MAX_WAIT: << parameters.max-wait >>
                    MC_API_URL: << parameters.api-url >>
                    POLL_INTERVAL: << parameters.poll-interval >>
                name: ⚠️ MC Prevent
                no_output_timeout: 6m
            - run:
                command: |
                    F=/tmp/mc-prevent-raw-response.json
                    if [ -f "$F" ]; then
                      jq . "$F" 2>/dev/null || cat "$F"
                    else
                      echo "No response available."
                    fi
                name: Raw API response (click to expand)
                when: always
            - save_cache:
                key: mc-prevent-{{ .Branch }}-{{ epoch }}
                paths:
                    - /tmp/mc-prevent-verdict.json
                when: always
executors:
    default:
        description: Default executor for MC Prevent.
        docker:
            - image: cimg/base:current
jobs:
    assess:
        description: |
            Run MC Prevent as a standalone job. Assesses data pipeline risk for the current pull request.
        executor: default
        parameters:
            api-url:
                default: https://api.getmontecarlo.com/ci/assess
                description: MC Prevent API URL.
                type: string
            block-on:
                default: ""
                description: |
                    Which risk tiers cause the job to exit non-zero: low+, medium+, high+, or none. When left unset the Monte Carlo UI setting applies.
                type: string
            fail-on:
                default: ""
                description: |
                    Deprecated — use block-on instead.
                type: string
            fail-on-error:
                default: true
                description: |
                    Deprecated — use block-on instead.
                type: boolean
            max-wait:
                default: 300
                description: Max seconds to wait.
                type: integer
            poll-interval:
                default: 30
                description: Seconds between poll attempts.
                type: integer
        steps:
            - assess:
                api-url: << parameters.api-url >>
                block-on: << parameters.block-on >>
                fail-on: << parameters.fail-on >>
                fail-on-error: << parameters.fail-on-error >>
                max-wait: << parameters.max-wait >>
                poll-interval: << parameters.poll-interval >>
examples:
    basic-usage:
        description: |
            Basic usage. MC Prevent runs on every PR and warns/fails on risky changes.
        usage:
            version: "2.1"
            orbs:
                mc-prevent: https://raw.githubusercontent.com/monte-carlo-data/mc-prevent-orb/main/orb.yml
            workflows:
                main:
                    jobs:
                        - mc-prevent/assess:
                            context:
                                - monte-carlo
                            filters:
                                branches:
                                    ignore:
                                        - main
                                        - master
                            name: mc-prevent
    custom-options:
        description: |
            Only block on high-risk PRs. Medium and low risk show in output but don't block.
        usage:
            version: "2.1"
            orbs:
                mc-prevent: https://raw.githubusercontent.com/monte-carlo-data/mc-prevent-orb/main/orb.yml
            workflows:
                main:
                    jobs:
                        - mc-prevent/assess:
                            block-on: high+
                            context:
                                - monte-carlo
                            filters:
                                branches:
                                    ignore:
                                        - main
                            name: mc-prevent

